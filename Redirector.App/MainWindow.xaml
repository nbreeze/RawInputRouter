<Window
    x:Class="Redirector.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Redirector.App"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Frame x:Name="ContentFrame"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Top" Height="36">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="36"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="1" x:Name="CustomDragRegion" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <TextBlock Margin="16,0" VerticalAlignment="Center" Text="{x:Bind Title}"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
